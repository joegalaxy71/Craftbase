{{define "products"}}
    {{ template "head" }}
    <html>
        <h1>{{ .Title }} - template PRODUCTS</h1>
        <h3>by {{ .Author }}</h3>
        {{ template "sub" }}
    </html>
    {{ template "scripts" }}
{{end}}
